import { ConcatenateToEllipsisPipe } from './concatenate-to-ellipsis.pipe';

describe('ConcatenateToEllipsisPipe', () => {
  it('create an instance', () => {
    const pipe = new ConcatenateToEllipsisPipe();
    expect(pipe).toBeTruthy();
  });
});
